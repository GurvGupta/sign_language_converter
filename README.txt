Make sure to have python installed in your system and working.

Now run "pip install -r requirements.txt" 
# Make sure to run this command in directory where requirements.txt file is present. (In the root folder of the project)
# It should be installing all the libraries mentioned in requirements file to run the project.

Now run "python -m streamlit run script.py"